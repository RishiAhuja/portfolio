
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=
PUBLIC_SUPABASE_ANON_KEY=
# Server-only (Vercel env, never PUBLIC_): secret/service role key for admin writes
SUPABASE_URL=
SUPABASE_SECRET_KEY=
PUBLIC_HASHNODE_USERNAME=rishi2220
# Required since May 2026: Hashnode GraphQL reads need Pro + a PAT from
# https://hashnode.com/settings/developer (use the raw token, no Bearer prefix).
HASHNODE_PERSONAL_ACCESS_TOKEN=
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=
R2_BUCKET_NAME=
PUBLIC_R2_PUBLIC_URL=
PUBLIC_SUPABASE_PROXY_URL=

# Lamatic Chat Widget (server/local environment only)
LAMATIC_API_KEY=
CLUSTER_PROGRESS_TOKEN=
# Optional: regenerate /graph with semantic edges (local script only)
OPENAI_API_KEY=
# Set to 1 with `npx chroma run` for local Chroma persistence during graph:build
USE_CHROMA=
CHROMA_URL=http://localhost:8000
